const db = require("../config/db");

// Define queries for user-related operations here
// Example:
// exports.findUserByEmail = (email, callback) => {
//     db.query('SELECT * FROM users WHERE email = ?', [email], callback);
// };
